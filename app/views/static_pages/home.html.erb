<%= form_tag("/search", method: "post") do %>
  <%= label_tag(:chicken, "Search for:") %>
  <%= text_field_tag(:chicken) %>
  <%= submit_tag "Search", id: "search_button" %>
<% end %>



<% if @bars %>
	<% @bars.each do |bar| %>
		<%= link_to "#{bar.name}, #{bar.address} #{bar.rating}"  ,  venue_path(bar) %><br>
	<% end %>
<%end%>
